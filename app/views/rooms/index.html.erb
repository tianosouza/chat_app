<%= turbo_frame_tag 'rooms_controller' do %>
  <main class="flex w-full h-screen bg-[#181D31]">
    <%= render 'shared/side_bar' %>

    <section class="flex flex-col items-center justify-center gap-5 h-screen w-full">
      <%= image_tag('01.png', width: 100) %>
      <h1 class="font-bold text-4xl text-white my-4">Chat App - Ruby on Rails</h1>
      <p class="text-center font-semibold text-white">The is the chat app with RoR</p>
    </section>
  </main>
<% end %>
